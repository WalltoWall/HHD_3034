import { style } from 'treat'

export const tiltRight = style({
  transform: 'rotate(4deg)',
})

export const tiltLeft = style({
  transform: 'rotate(-4deg)',
})

export const shadow = style({
  filter: 'drop-shadow(0 3px 1.5px rgba(0, 0, 0, 0.4))',
})
